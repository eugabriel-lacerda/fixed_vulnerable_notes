services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-vuln_notes}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-vuln_notes}
      POSTGRES_DB: ${DB_NAME:-vuln_notes}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-vuln_notes} -d ${DB_NAME:-vuln_notes}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
